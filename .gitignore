# === Dependencies & package managers ===
node_modules/
.pnp/
.pnp.*
# Yarn
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
# PNPM
pnpm-lock.yaml
# Bun cache (lockfile should be committed, cache not)
bun.lockb

# === Build / caches ===
.next/
out/
dist/
.eslintcache
.turbo/
.cache/
coverage/
*.tsbuildinfo

# === OS junk ===
.DS_Store
Thumbs.db

# === Env & secrets (NEVER commit real keys) ===
.env
.env.local
.env.development
.env.development.local
.env.production
.env.production.local
.env.test
.env.test.local
# allow examples
!.env.example
!.env.local.example

# === Vercel ===
.vercel

# === Next.js generated ===
next-env.d.ts

# === Logs ===
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
bun-debug.log*

# === Public uploads (keep folder, ignore files) ===
public/uploads/**
!public/uploads/**/.gitkeep

# === Supabase local artifacts (keep migrations, ignore temp) ===
supabase/.temp/**
supabase/.branches/**
supabase/.cache/**
